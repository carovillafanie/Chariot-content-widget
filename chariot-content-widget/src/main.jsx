import React from "react";
import ReactDOM from "react-dom/client";
import ContentGridWidget from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ContentGridWidget />
  </React.StrictMode>
);
